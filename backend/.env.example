# Copy values for local development; use strong secrets in production.
PORT=
NODE_ENV=

# PostgreSQL (only DATABASE_URL is used by the app)
DATABASE_URL=

JWT_SECRET=
JWT_EXPIRES_IN=

# Comma-separated origins for CORS + Socket.IO (admin Vite default shown)
CORS_ORIGIN=
OPENAI_BASE_URL=
